import { MovescrollDirective } from './movescroll.directive';

describe('MovescrollDirective', () => {
  it('should create an instance', () => {
    const directive = new MovescrollDirective();
    expect(directive).toBeTruthy();
  });
});
